import { WebGPURenderer } from "three/webgpu";

// Uncomment the next line to cause a build error. When it is commented, there is no error because threejs does not have a top-level await.
// await null;

console.log(WebGPURenderer);
